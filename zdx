local C = {
    P = game:GetService("Players"),
    RS = game:GetService("RunService"),
    UIS = game:GetService("UserInputService"),
    HS = game:GetService("HttpService"),
    CG = game:GetService("CoreGui"),
    lp = game:GetService("Players").LocalPlayer
}

local char, hum, hrp
local function upd() 
    char = C.lp.Character or C.lp.CharacterAdded:Wait()
    hum = char:FindFirstChildOfClass("Humanoid")
    hrp = char:FindFirstChild("HumanoidRootPart")
end
upd()

-- Configuration & Save System
local CFG_FILE = "TimCheeseConfig.json"
local Config = {
    Float = false, FloatH = 5, FloatS = 3,
    Fly = false, FlyY = 25, FlyS = 35,
    AntiDie = false
}

local function saveSettings()
    if writefile then writefile(CFG_FILE, C.HS:JSONEncode(Config)) end
end

local function loadSettings()
    if isfile and isfile(CFG_FILE) then
        local ok, data = pcall(C.HS.JSONDecode, C.HS, readfile(CFG_FILE))
        if ok then for k, v in pairs(data) do Config[k] = v end end
    end
end
loadSettings()

-- Anti-Die Logic (Directly from your file)
local function applyAntiDie()
    if not hum or not Config.AntiDie then return end
    hum.BreakJointsOnDeath = false
    hum:SetStateEnabled(Enum.HumanoidStateType.Dead, false)

    local connection
    connection = hum:GetPropertyChangedSignal("Health"):Connect(function()
        if Config.AntiDie and hum.Health <= 0 then
            hum.Health = hum.MaxHealth
        elseif not Config.AntiDie then
            connection:Disconnect()
        end
    end)
end

local function doTpDown()
    if not hrp then return end
    hrp.CFrame = CFrame.new(hrp.Position.X, -8.8, hrp.Position.Z)
end

-- Re-acquire on respawn
C.lp.CharacterAdded:Connect(function(newChar)
    char = newChar
    hum = newChar:WaitForChild("Humanoid")
    hrp = newChar:WaitForChild("HumanoidRootPart")
    if Config.AntiDie then
        task.defer(applyAntiDie)
    end
end)

-- Draggable Logic
local function makeDraggable(frame)
    local dragging, dragInput, dragStart, startPos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true dragStart = input.Position startPos = frame.Position
            input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
        end
    end)
    C.UIS.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

-- UI Construction
local sg = Instance.new("ScreenGui", C.CG)
sg.Name = "TimCheeseHub"
sg.ResetOnSpawn = false

-- Box container for Float + Gear
local box = Instance.new("Frame", sg)
box.Size = UDim2.new(0, 108, 0, 42)
box.Position = UDim2.new(0.5, -54, 0.4, 0)
box.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
box.Active = true
Instance.new("UICorner", box).CornerRadius = UDim.new(0, 8)
makeDraggable(box)

-- RGB outline
local boxStroke = Instance.new("UIStroke", box)
boxStroke.Thickness = 2.5
boxStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
C.RS.RenderStepped:Connect(function()
    boxStroke.Color = Color3.fromHSV(tick() % 5 / 5, 1, 1)
end)

-- Float button inside box
local floatBtn = Instance.new("TextButton", box)
floatBtn.Size = UDim2.new(0, 70, 0, 30)
floatBtn.Position = UDim2.new(0, 6, 0.5, -15)
floatBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
floatBtn.Text = "Float"
floatBtn.TextColor3 = Color3.new(1, 1, 1)
floatBtn.Font = Enum.Font.GothamBold
floatBtn.TextSize = 14
Instance.new("UICorner", floatBtn).CornerRadius = UDim.new(0, 6)

local function updateFloatBtn()
    floatBtn.BackgroundColor3 = Config.Float and Color3.fromRGB(45, 130, 45) or Color3.fromRGB(35, 35, 35)
end
updateFloatBtn()

floatBtn.MouseButton1Click:Connect(function()
    Config.Float = not Config.Float
    updateFloatBtn()
    saveSettings()
end)

-- Gear button inside box (next to float)
local gearBtn = Instance.new("TextButton", box)
gearBtn.Size = UDim2.new(0, 26, 0, 30)
gearBtn.Position = UDim2.new(0, 78, 0.5, -15)
gearBtn.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
gearBtn.Text = "⚙"
gearBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
gearBtn.Font = Enum.Font.GothamBold
gearBtn.TextSize = 16
Instance.new("UICorner", gearBtn).CornerRadius = UDim.new(0, 6)

-- Settings panel
local settingsPanel = Instance.new("Frame", sg)
settingsPanel.Size = UDim2.new(0, 170, 0, 204)
settingsPanel.Position = UDim2.new(0.5, -85, 0.4, 48)
settingsPanel.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
settingsPanel.Visible = false
settingsPanel.Active = true
Instance.new("UICorner", settingsPanel).CornerRadius = UDim.new(0, 8)
makeDraggable(settingsPanel)

local spStroke = Instance.new("UIStroke", settingsPanel)
spStroke.Thickness = 2.5
spStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
C.RS.RenderStepped:Connect(function()
    spStroke.Color = Color3.fromHSV(tick() % 5 / 5, 1, 1)
end)

local stitle = Instance.new("TextLabel", settingsPanel)
stitle.Size, stitle.Text = UDim2.new(1, 0, 0, 24), "Settings"
stitle.TextColor3, stitle.BackgroundTransparency = Color3.fromRGB(255, 215, 0), 1
stitle.Font, stitle.TextSize = Enum.Font.GothamBold, 13

local closeBtn = Instance.new("TextButton", settingsPanel)
closeBtn.Size, closeBtn.Position = UDim2.new(0, 22, 0, 22), UDim2.new(1, -24, 0, 1)
closeBtn.BackgroundTransparency, closeBtn.Text = 1, "✕"
closeBtn.TextColor3, closeBtn.Font, closeBtn.TextSize = Color3.fromRGB(255, 80, 80), Enum.Font.GothamBold, 13
closeBtn.MouseButton1Click:Connect(function() settingsPanel.Visible = false end)

local function makeRow(parent, label, yOff, value, onChange)
    local lbl = Instance.new("TextLabel", parent)
    lbl.Size, lbl.Position = UDim2.new(0.5, 0, 0, 24), UDim2.new(0, 8, 0, yOff)
    lbl.Text, lbl.TextColor3 = label, Color3.new(0.7, 0.7, 0.7)
    lbl.BackgroundTransparency, lbl.Font, lbl.TextSize, lbl.TextXAlignment = 1, Enum.Font.Gotham, 11, Enum.TextXAlignment.Left

    local box = Instance.new("TextBox", parent)
    box.Size, box.Position = UDim2.new(0, 50, 0, 22), UDim2.new(1, -58, 0, yOff + 1)
    box.BackgroundColor3, box.Text, box.TextColor3 = Color3.fromRGB(40, 40, 40), tostring(value), Color3.new(1, 1, 1)
    box.Font, box.TextSize = Enum.Font.Gotham, 12
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 6)
    box.FocusLost:Connect(function()
        local v = tonumber(box.Text)
        if v then onChange(v) saveSettings() else box.Text = tostring(value) end
    end)
    return box
end

makeRow(settingsPanel, "Float Height", 28, Config.FloatH, function(v) Config.FloatH = v end)
makeRow(settingsPanel, "Float Speed", 56, Config.FloatS, function(v) Config.FloatS = v end)
makeRow(settingsPanel, "Fly Height", 84, Config.FlyY, function(v) Config.FlyY = math.max(1, v) end)
makeRow(settingsPanel, "Fly Speed", 112, Config.FlyS, function(v) Config.FlyS = math.max(1, v) end)

local flyBtn = Instance.new("TextButton", settingsPanel)
flyBtn.Size, flyBtn.Position = UDim2.new(1, -16, 0, 24), UDim2.new(0, 8, 0, 140)
flyBtn.Font, flyBtn.TextSize, flyBtn.TextColor3 = Enum.Font.GothamBold, 12, Color3.new(1, 1, 1)
Instance.new("UICorner", flyBtn).CornerRadius = UDim.new(0, 6)

local function updateFlyBtn()
    flyBtn.Text = "Fly: " .. (Config.Fly and "ON" or "OFF")
    flyBtn.BackgroundColor3 = Config.Fly and Color3.fromRGB(45, 90, 140) or Color3.fromRGB(35, 35, 35)
end
updateFlyBtn()

flyBtn.MouseButton1Click:Connect(function()
    Config.Fly = not Config.Fly
    updateFlyBtn()
    saveSettings()
end)

-- Anti-Die toggle in settings
local adBtn = Instance.new("TextButton", settingsPanel)
adBtn.Size, adBtn.Position = UDim2.new(1, -16, 0, 24), UDim2.new(0, 8, 0, 28 + 140)
adBtn.Font, adBtn.TextSize, adBtn.TextColor3 = Enum.Font.GothamBold, 12, Color3.new(1, 1, 1)
Instance.new("UICorner", adBtn).CornerRadius = UDim.new(0, 6)

local function updateAD()
    adBtn.Text = "Anti-Die: " .. (Config.AntiDie and "ON" or "OFF")
    adBtn.BackgroundColor3 = Config.AntiDie and Color3.fromRGB(45, 90, 45) or Color3.fromRGB(35, 35, 35)
end
updateAD()

adBtn.MouseButton1Click:Connect(function()
    Config.AntiDie = not Config.AntiDie
    updateAD()
    saveSettings()
    if Config.AntiDie then applyAntiDie() end
end)

gearBtn.MouseButton1Click:Connect(function()
    settingsPanel.Visible = not settingsPanel.Visible
end)

-- Logic Loop
local fCount = 0
C.RS.Heartbeat:Connect(function(dt)
    if not hrp or not hum then upd() return end

    if Config.Fly then
        local targetY = math.max(1, Config.FlyY)
        local flySpeed = math.max(1, Config.FlyS)

        if hrp.Position.Y < targetY then
            local velocity = hrp.AssemblyLinearVelocity
            hrp.AssemblyLinearVelocity = Vector3.new(velocity.X, flySpeed, velocity.Z)
        else
            local velocity = hrp.AssemblyLinearVelocity
            hrp.AssemblyLinearVelocity = Vector3.new(velocity.X, 0, velocity.Z)
            doTpDown()
        end
    elseif Config.Float then
        fCount = fCount + dt * Config.FloatS
        hrp.AssemblyLinearVelocity = Vector3.new(hrp.AssemblyLinearVelocity.X, 0, hrp.AssemblyLinearVelocity.Z)
        hrp.CFrame = hrp.CFrame * CFrame.new(0, math.sin(fCount) * (Config.FloatH * 0.05), 0)
    end
end)

if Config.AntiDie then applyAntiDie() end
